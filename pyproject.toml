[project]
name = "diversificador"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cvxopt>=1.3.2",
    "dadosabertosbrasil>=2.0.1",
    "dash>=3.3.0",
    "dash-bootstrap-components>=2.0.4",
    "yfinance>=1.0",
]

[dependency-groups]
dev = [
    "pytest-cov>=7.0.0",
]
